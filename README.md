
| 필드명     | 설명       | 타입              | 제약 조건     |
|------------|------------|-----------------|----------------|
| `todoID`   | 기본 키     | `Long`          | Primary Key    |
| `제목`     | 일정 제목   | `String`        | -              |
| `내용`     | 일정 내용   | `String`        | -              |
| `작성자명` | 작성자 이름 | `String`        | -              |
| `이메일`   | 작성자 이메일 | `String`        | -              |
| `비밀번호` | 작성자 비밀번호 | `String`        | -              |
| `작성일`   | 생성 시각   | `LocalDateTime` | 자동 생성 (`@CreatedDate`) |
| `수정일`   | 수정 시각   | `LocalDateTime` | 자동 수정 (`@LastModifiedDate`) |

Lv1
- [ ]  일정을 생성, 조회, 수정, 삭제할 수 있습니다.
- [ ]  일정은 아래 필드를 가집니다.
- [ ]  `작성 유저명`, `할일 제목`, `할일 내용`, `작성일`, `수정일` 필드
- [ ]  `작성일`, `수정일` 필드는 `JPA Auditing`을 활용합니다

Lv2
- [ ]  유저를 생성, 조회, 수정, 삭제할 수 있습니다.
- [ ]  유저는 아래와 같은 필드를 가집니다.
- [ ]  `유저명`, `이메일`, `작성일` , `수정일` 필드
- [ ]  `작성일`, `수정일` 필드는 `JPA Auditing`을 활용합니다.
- [ ]  연관관계 구현
- [ ]  일정은 이제 `작성 유저명` 필드 대신 `유저 고유 식별자` 필드를 가집니다.
- 
- [+]  `패스워드`와 `이메일` 변경 가능성이 있어 따로 기능 추가로 변경 가능하게 만듬
Lv3 
- 일이 꼬였다 덕분에 Lv2가 LV3처럼 되어버렸다.
- - [ ]  유저에 `비밀번호` 필드를 추가합니다.
- 비밀번호 암호화는 도전 기능에서 수행합니다.
- 인데 이미 기능이 다 구현되어 있다...
